// Scale bounds (10^-16 to 10^26 meters)
export const MIN_LOG_SCALE = -16; // Quarks
export const MAX_LOG_SCALE = 26; // Observable universe

// Camera animation
export const CAMERA_SMOOTHING = 0.1;

// Zoom input
export const WHEEL_SPEED = 0.3; // Orders of magnitude per scroll tick
export const PINCH_SPEED = 0.15; // Slower for trackpad pinch

// Layer crossfade
export const FADE_RANGE = 1; // Orders of magnitude for fade transition
